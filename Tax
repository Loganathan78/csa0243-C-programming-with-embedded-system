#include<stdio.h>
#include<conio.h>
void main()
{
 float n, s1, s2, s3;
clrscr();
printf("Enter the salary:\n");
scanf("%f",&n);
 s1=(n*0.10);
 s2=(n*0.20);
 s3=(n*0.30);
if(n<=1000000)
{
 printf("the tax amount of salary is %f",s1);
}
 else if(n>=2000000)
 printf("the tax amount of salary is %f",s2);
 else
 printf("the tax amount of salary is %f",s3);
}
